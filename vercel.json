{
    "version": 2,
    "buildCommand": "npm run build --configuration=production",
    "installCommand": "npm install",
    "outputDirectory": "dist/angular-ecomm",
    "routes": [
      {
        "src": "/.*",
        "dest": "/index.html"
      }
    ]
  }